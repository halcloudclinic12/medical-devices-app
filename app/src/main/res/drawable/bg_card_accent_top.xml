<?xml version="1.0" encoding="utf-8"?>
<!-- Thin accent strip along the top of the featured Body Checkup card. Rounded only on
     top so it sits flush with the card's own rounded corners (card clips to its outline). -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <solid android:color="@color/dash_featured_border" />
    <corners
        android:topLeftRadius="18dp"
        android:topRightRadius="18dp"
        android:bottomLeftRadius="0dp"
        android:bottomRightRadius="0dp" />

</shape>
